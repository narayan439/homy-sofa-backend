/*
 * BookingSequenceRepository was used with a BookingSequence JPA entity that
 * attempted to create a 'booking_sequence' table. The sequence entity has
 * been converted to a plain POJO; if you still need a sequence table manage
 * it explicitly via SQL migration and reintroduce a proper entity.
 */
// Repository intentionally removed - no sequence entity in use.

